
# Absolute public origin, no trailing slash. Used for canonical URLs, og:url
# and sitemap.xml — all of which are ignored by crawlers unless absolute, so
# this must be the real domain in production.
VITE_SITE_URL={{SITE_URL}}
