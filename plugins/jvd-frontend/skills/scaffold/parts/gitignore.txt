# Yarn 4: the release binary and plugins are committed, its caches are not.
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.pnp.*

# Prerender SSR output
dist-ssr

# Generated by vite-plugin-pwa at build time
dev-dist
